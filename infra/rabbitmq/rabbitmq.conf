# RabbitMQ configuration — loaded from /etc/rabbitmq/rabbitmq.conf
# Enables loading of definitions.json on boot (so exchanges/queues/bindings
# declared in definitions.json are created automatically).

management.load_definitions = /etc/rabbitmq/definitions.json